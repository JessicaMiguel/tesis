package com.example.tesis.repository;
import org.springframework.stereotype.Repository;

/**
 * Interface del repositorio del archivo en crudo que se sube a la plataforma.
 */
@Repository
public interface FileRepository {
}